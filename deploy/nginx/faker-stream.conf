# Include this file inside the top-level stream {} block.
# Mock TCP is raw ISO8583 over TCP/TLS, so it must use Nginx stream instead of HTTP location.

server {
    listen 14400;
    proxy_pass 127.0.0.1:15400;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14401;
    proxy_pass 127.0.0.1:15401;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14402;
    proxy_pass 127.0.0.1:15402;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14403;
    proxy_pass 127.0.0.1:15403;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14404;
    proxy_pass 127.0.0.1:15404;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14405;
    proxy_pass 127.0.0.1:15405;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14406;
    proxy_pass 127.0.0.1:15406;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14407;
    proxy_pass 127.0.0.1:15407;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14408;
    proxy_pass 127.0.0.1:15408;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}

server {
    listen 14409;
    proxy_pass 127.0.0.1:15409;
    proxy_connect_timeout 10s;
    proxy_timeout 300s;
}
